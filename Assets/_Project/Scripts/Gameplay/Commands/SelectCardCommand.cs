using QFramework;

namespace Unomata.Gameplay
{
    /// <summary>
    /// 选牌指令，将玩家 UI 点击的选项索引传入 HackSession。
    /// TODO (Phase 4): 填充选牌逻辑——调用 HackSystem.CurrentSession.SelectOption(index)。
    /// </summary>
    public class SelectCardCommand : AbstractCommand
    {
        protected override void OnExecute()
        {
            // TODO (Phase 4): 填充选牌逻辑
        }
    }
}
