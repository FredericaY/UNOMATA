# UNOMATA Agent Rules

## 项目概览
双线并行TPS验证项目。主线：TPS竞技场；副线：接龙小游戏（UNO改编）。
- Unity 2022.3 LTS + URP + QFramework
- 底层逻辑纯C#，通过事件/委托与Unity端解耦
- 详见 Docs/ 目录下各文档

## Unity 操作
- 可以且应当使用 UnityMCP 直接在 Unity 中执行操作（创建对象、修改组件、运行脚本等）
- 操作前先确认 Unity Editor 已打开且处于非运行状态
- 涉及 Scene 修改后提示用户保存场景

## 目录约定
- 项目自有代码：Assets/_Project/Scripts/
- 底层C#逻辑：Assets/_Project/Scripts/Core/（不含任何 UnityEngine 依赖）
- Unity端逻辑：Assets/_Project/Scripts/Gameplay/
- 双线联动层：Assets/_Project/Scripts/Gameplay/Linking/
- 第三方资产：Assets/ThirdParty/（只读，不修改）
- 开发期独立控制台项目：CardChainCore/

## 代码规范
- 命名空间：Unomata.Core（底层）/ Unomata.Gameplay（Unity端）/ Unomata.UI
- 类名/方法名：PascalCase；字段：camelCase；私有字段加下划线前缀 _camelCase
- 底层模块（Core）严禁引用 UnityEngine，保持纯C#可测试性
- 事件用 C# 原生 event Action / event Action<T>，不用 UnityEvent
- QFramework 架构层：用 Architecture、System、Model、Command、Query 规范分层
- 单个文件不超过300行，超出则拆分
- 禁止魔法数字，常量统一放对应模块的 Config 类

## 响应规范（节省Token）
- 读文件前先用 grep/glob 确认文件存在，避免无效读取
- 修改现有文件时只展示变更部分，不重复输出未修改内容
- 已知项目结构时不重复扫描目录
- 分析问题时直接给结论，不逐步复述已知信息
- 代码块只包含必要上下文（前后各3行），不输出整个文件

## 禁止事项
- 不修改 Assets/ThirdParty/ 下任何文件
- 不在 Core 层引入 Unity 依赖
- 不绕过 INTERFACE.md 约定的接口直接跨层调用
- 不自行创建文档文件（.md），除非用户明确要求
