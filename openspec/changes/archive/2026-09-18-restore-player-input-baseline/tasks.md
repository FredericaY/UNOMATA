## 1. 保存起点与恢复输入资产

- [x] 1.1 记录实施前工作区、输入资产 GUID/原动作与绑定、场景接线及已有 Console 证据；通过 MCP 核对工程、版本和就绪状态，确认用户修改与活动 IK 文件均在保留清单中。
- [x] 1.2 在项目输入资产的 Player Map 持久化 Aim/Fire 与鼠标右/左键及 KeyboardMouse 分组；对比源 JSON，验证六动作齐全、原四动作 ID/类型/绑定和项目 GUID 不变，供应商文件无差异。
- [x] 1.3 核对 PlayerArmature 引用项目副本、defaultActionMap=Player、InvokeCSharpEvents；保存必要的 Editor 修改后强制重导入并完成独立 Editor 重载，验证导入资产与运行实例仍能解析六动作。

## 2. 修复输入接收生命周期

- [x] 2.1 实现当前 PlayerInput 动作实例的完整校验与幂等绑定，处理初次 OnEnable/Start 时序；用缺引用、缺 Map、缺 Aim/Fire、类型不兼容的隔离对象验证不留下部分订阅，诊断含完整缺项且不逐帧刷屏。
- [x] 2.2 为 PlayerController 实现安全退订及经 Command 的中立复位，覆盖禁用、销毁、PlayerInput/Map 停用与失焦；验证未 Start 即禁用、绑定失败后销毁均无异常，也不会为清理创建 GameApp。
- [x] 2.3 实现恢复后重新绑定/采样与 Jump/Fire 释放门槛；对组件及输入源连续启停三次，验证新输入可用、旧按下不重放、每次变化只处理一次。
- [x] 2.4 按真实值处理 Sprint 及按钮释放，保留现有 Sprint PassThrough 类型；通过按下→归零回调验证 Sprint=false，验证 Fire 左键松开为 false 且没有射击副作用。

## 3. 统一瞄准状态链路

- [x] 3.1 修改 SetAimStateCommand 仅写 PlayerInputModel.IsAiming，由 PlayerSystem 同步 PlayerModel 与事件；对 true/true/false/false 验证两个 Model 一致且仅有两个转换事件。
- [x] 3.2 保存并在 PlayerSystem.OnDeinit 清理输入订阅，初始化同步当前值；验证系统释放后旧订阅不再响应，新上下文只有一份订阅，输入复位可驱动相机/动画退出瞄准。

## 4. 修复第三方输入适配

- [x] 4.1 将运动缓冲准备放到 TPC 消费前，Jump 按边沿投递而不重注入已消费请求；用帧号验证当前帧 Move/Sprint 可被消费，按住 Jump 跨一次起落不自动连跳，释放重按可以再次跳。
- [x] 4.2 对 Look 直通建立唯一且成对管理的订阅，适配器/输入源停用与失焦时清空 SA 缓冲；验证鼠标位移不累加、停止后归零、重新启用无重复写入，不改供应商脚本与全局输入设置。

## 5. 可重复回归与实际场景验收

- [x] 5.1 在项目 Editor/Validation 下建立显式运行的输入验证入口和结果记录；验证能通过虚拟设备/真实动作回调穿过 Controller，并在成功、失败、中止时清理临时设备、对象、动作副本和订阅，不默认挂入 SampleScene。
- [x] 5.2 执行完整的资产持久化及配置故障矩阵；验证每一种必要引用/Map/动作缺失或类型错误都可诊断，修复后重新启用正常，失败与清理均无 NullReferenceException。
- [x] 5.3 执行业务五输入与 Look 的按下/释放、现有手柄绑定非回退、首次禁用、三次重复启停、输入源停用、失焦外释放及重新输入；记录 Model/缓冲/事件数，确认无卡键、重复订阅或旧请求重放。
- [x] 5.4 在至少两个独立 Play 会话中验证 SampleScene 实际连续推进至少 120 帧，并实测键鼠移动/跳跃/冲刺、瞄准相机切换、动画权重与脚步/落地触发；记录 Console 和观察边界，不把程序化注入当作手感已验收。
- [x] 5.5 按设计 D7 定向复现首次运行的 Missing Script 和停在首帧现象，取得可复查的归因证据；输入范围内问题修复并回归，范围外问题登记独立入口，仍不明或影响输入验收时保持相应任务未完成，整体 R2 不得虚报通过。

## 6. 文档与交付核对

- [x] 6.1 将实际路由、释放语义、验证入口和本轮通过/受阻结果写入 Docs/INPUT_BASELINE.md，并同步 ARCHITECTURE、ENVIRONMENT_RECOVERY、TODO 与双语 README；核对所有声明有本轮证据，旧归档和 IK 勾选不被改写。
- [x] 6.2 运行本 change 严格规格校验和受影响检查，核对源码/资产/meta 配对、无供应商或无关包改动、无遗留临时对象；只有实现及验收均完成后才准备 delta 同步与归档，不以规划状态或历史 139 项 Core 通过代替输入验收。

## 7. Audio 场景残留清理（2026-09-18 用户补充范围）

- [x] 7.1 在非 Play Mode 下备份并核对场景，优先使用 Editor 移除 Audio 上误保存的运行时退订组件，常规 API 已证实无法处理时按 D8 的受限例外清理并由 Editor 重载；验证 diff 仅涉及残留组件/内嵌脚本，原 AudioBridge、两份 AudioSource 和所有引用保持不变。
- [x] 7.2 重新加载场景，完成至少两次独立 Play/Stop；验证对应 Missing Script 消失、运行辅助组件按需生成，停止后场景不重新持久化它且无新增异常。
- [x] 7.3 验证音频模型资源、两个 AudioSource 和既有脚步/落地播放端有效，将清理证据同步到三个 delta 所对应的产物与共享文档；不将程序化验证冒充真实听感验收。

## 2026-09-18 实施记录

- 150 个自动回归断言、15 种故障配置通过；成功/失败/Cancel 清理通过，源资产重载持久化、编译、元数据与差异检查通过。
- 4.1 的真实 TPC 同帧/落地验证已完成；5.3/5.4 原有人工确认项现按 2026-09-18 用户整体恢复基线验收收口；未补写不存在的物理键鼠逐项日志。
- 5.5 已定位并登记 R2-AUDIO-01：Audio 的序列化运行时退订组件残留。用户后续已明确要求纳入当前 change，新增 7.1–7.3 跟踪清理；记录见 Docs/INPUT_BASELINE.md。
- 三个 delta 已同步主 specs；本变更在阶段验收后归档，原 IK 变更保持 22/37。

- Audio 追加清理已完成 7.1：仅删除 28 行，其他 69 个场景块与 .meta 保持不变，Editor 重载后仅剩四个有效组件。MCP 曾在首次尝试后失联，该次不计通过；连接恢复后重新执行两次可确认运行与音频复验，7.2 / 7.3 已通过。

- 清理复验：两次独立 Play/Stop 与最终 Console 均零错误/警告；运行态 0 空槽、1 辅助组件，退出后仅四个持久组件且场景无脏修改。
- 4.1 场景验证：帧 2→1286，同帧 Move/Sprint 消费成立，按住跳跃第一次完整起落后未连跳，释放重按完成第二次起落；脚步/落地播放均被观察到。第二次独立运行额外推进 180 帧，设备及临时设置恢复。
- 程序化验证结束时曾为 19/21；本轮用户确认恢复基线可接受并要求收口后为 21/21。自动/场景证据与用户阶段验收分开记录，不虚构额外手工测试。

## 2026-09-18 用户验收

用户确认项目已经回到当初暂停开发时的阶段，并明确要求任务勾选、文档同步、归档、提交和推送。5.3/5.4 以现有自动/场景验证加本轮用户整体阶段验收收口；没有新增逐项物理键鼠记录，不将模拟失焦或程序化输入写成这种记录。瞄准动画偏差后续另开 change，未纳入本次“已修复”结论。
