## 1. 注册生命周期与胶囊兼容

- [x] 1.1 在 EnemySystem 增加提交后的注册/注销类型事实；用事件内查询验证状态顺序，覆盖失败注册、重复注销、重新注册及旧身份无效，并重跑既有伤害公式/去重/死亡验证。
- [x] 1.2 将 EnemyController 限定为配置/碰撞注册与生命周期适配，移出 Renderer 隐藏；验证死亡/注销同步关闭所有命中体、失败注册不成为有效目标，且清理不重建架构。
- [x] 1.3 建立表现与 UI 共用的身份绑定约定，采用订阅后查询及代际校验；验证 Controller/View 两种启用顺序、仅重启 Controller 和晚启用 View 均取得正确快照。
- [x] 1.4 为 EnemyCapsule 迁移立即隐藏的项目表现配置，通过 Editor 保存 Prefab 和受影响的胶囊场景引用；验证原胶囊死亡立即隐藏、重新启用恢复以及旧 _renderers 接线没有双重写入者。

## 2. 机甲资产与动画表现

- [x] 2.1 通过 Unity Editor 核对 mech_defender 的实际导入骨架、clip 长度/事件和材质，制作项目包装及配置；记录源路径、缩放、贴地和躯干命中体标定，运行确认无绑定/材质错误且第三方无修改。
- [x] 2.2 创建项目 Idle/Hit/Death 控制器并实现待机/非致死受击播放；验证循环待机无根漂移、正扣血才开始 Hit、连续命中仍扣血但不重启/排队动作，零扣血保留物理命中反馈。
- [x] 2.3 实现一次性死亡动画和完成隐藏、缺动作/无法完成时的有限兜底；验证受击中致死立即关闭碰撞/UI、Death 不返回 Idle、离屏死亡能结束、后方目标可命中且旧死亡特效次数不增加。
- [x] 2.4 实现表现重置及禁用/销毁清理，清空旧身份的动作、计时和锁存；验证死亡中显式重置后超过旧死亡时长仍存活可见，单独恢复 View 不回放历史受击/死亡。

## 3. 敌人状态 UI

- [x] 3.1 实现基于只读快照的 HP 比例及减伤/易伤显示投影，复用现有数值规则；独立预期表覆盖 R=0.95、H=0/0.5/1/1.2/1.6、零扣血、极大合法 H，验证没有 NaN/Infinity 或显示导致的状态修改。
- [x] 3.2 制作头顶 UGUI/TMP 血条及防护标签，绑定现有字体和当前身份事件；运行验证每目标独立、文字字形完整、H 覆盖不改变 HP、受伤/重置后最迟下一次渲染同步，所有 UI 不拦截输入和射线。
- [x] 3.3 实现最终相机之后的 UI 朝向、投影、距离及遮挡判定；实际验证瞄准切换、墙后/离屏/背面/超距隐藏、重新可见时恢复，以及玩家/目标自身和 Trigger 过滤、查询饱和不漏墙。
- [x] 3.4 实现 UI 缺项诊断和订阅/显示清理；验证缺相机/锚点/显示引用时安全隐藏且伤害继续，修复后读取当前 HP，三次启停和场景退出无残留或重复订阅。

## 4. 场景与验证工具接入

- [x] 4.1 调整 ShootingSceneSetup 的胶囊入口并添加独立机甲 setup，避免旧工具覆盖新 SampleScene；验证重复运行入口不会增加重复目标或回退目标类型，必要拒绝信息明确且原胶囊生成仍可用。
- [x] 4.2 创建 Sandbox_EnemyPresentation，放置相同项目玩家/机甲、相机、方向光及并排/前后目标和墙面；实际试射验证同种目标独立状态、最近实体阻挡、前敌死亡后后敌命中，正常启动不自动扣血或设置 H。
- [x] 4.3 将 SampleScene 三处目标替换为项目机甲并显式绑定相机和 UI，保持 HP=100、R=0.95、H=0/0.5/1.2 对照；保存重载后验证脚底高度、目标 Layer、引用和约 1/10.5/24 的单发实际扣血。
- [x] 4.4 分离胶囊与机甲的保存审计，复用公共角色/声音检查并保留原胶囊断言；验证两类场景指定 Prefab、meta/GUID、动作/UI/命中体配置以及无运行时/测试对象保存残留。

## 5. 集成与视觉验收

- [x] 5.1 在实际 30/60/120fps 下运行机甲待机、连续受击、致死、死亡中重置和镜头移动；按真实 clip 时长采样并记录动作完成、HP、碰撞及显示时序，不用单帧赋值替代播放验证。
- [x] 5.2 执行实际 UI 数值与可见性矩阵，覆盖单目标 H 覆盖、晚绑定、死亡/注销、墙后/视口边界/近远距离；保留画面及独立预期对照，确认文字可读且不挡准星。
- [x] 5.3 执行 Controller/动画 View/UI 各三次启停、失效身份、死亡离屏和缺配置故障矩阵；单列预期诊断，检查旧事件/计时不影响新身份、对象数量有界及无未解释 Console 错误/警告。
- [x] 5.4 在 Sandbox_Shooting 实际重跑既有领域、物理、完整输入/反馈回归，在机甲场景执行站立、八方向行走、纯向前跑及移动跳落试射；记录本次枪口/双手与反馈结果，保持原阈值，若触及玩家/音频则追加对应完整回归。
- [x] 5.5 完成两次独立 Play/Stop、保存重载与场景退出检查；确认无残留血条/订阅、碰撞与初始状态可恢复、原素材及包无变更，形成可复核的本次报告。
- [x] 5.6 提供待机、单发/连发受击、死亡、血条/减伤/易伤和遮挡的实际试玩入口或录像，取得用户对模型尺寸、动作节奏、UI 与既有反馈衔接的视觉验收；记录确认来源，不以自动检查代替。

## 6. 文档与交付收口

- [x] 6.1 按实际实现同步 ARCHITECTURE、INTERFACE 和 GAME_DESIGN，说明新增生命周期事实及动画/UI 所有权；检查 AI、波次、骇入生成/计时仍列为未实现，消费端伤害规则未改变。
- [x] 6.2 在 DEPENDENCIES 登记实际模型、动画、材质/字体来源及派生资产，在敌人专项验收文档记录本次结果并更新 Docs/README 索引；核对路径与证据，不重写旧射击或其他归档验收记录。
- [x] 6.3 同步 TODO、DEVELOPMENT_PLAN 与 README 英中两部分，按验收事实更新 B3a 状态和后续 AI/波次顺序；检查本地链接、未决产品事项及 Git 授权边界一致。
- [x] 6.4 完成实现、资产、规格和报告差异复核及 OpenSpec 严格校验；确认范围内任务和用户验收完成后才准备同步三份 delta 与归档，缓存不入库，提交/推送遵循单独授权。

## Implementation record — 2026-09-19

本次完成项目机甲、动画、血条/防护标签、注册/注销事实与 View 生命周期，并保留胶囊基线和独立机甲验证场景。实际验证：新领域 25 项；敌人 30/60/120fps 各 66 项；原领域/物理 81/9 项；胶囊和机甲完整输入射击各 28 项；玩家动作 30 类、460 发；配置/实际命中补验 13 项。保存审计：机甲正式/独立场景 64/83 项、射击 37 项、既有玩家 79 项。两次正常 Play/Stop 各推进 180 帧，运行及退出 Console 无错误/警告。

源代码、运行、资产和共享文档已按当前事实同步。报告与失败诊断边界见 Docs/ENEMY_PRESENTATION.md 和忽略目录 .utmp/enemy-presentation。实施交付当时仅勾选已真实验证项，5.6 的用户视觉认可和 6.4 的归档收口保留待确认；当时未同步主规格、归档、提交或推送。后续用户验收和资产整理收口见本文件末尾。

最终复核补充：嵌套碰撞所有权保护专项 4 项、修正后 60fps 敌人矩阵 66 项、胶囊兼容 3 项及最后一次 180 帧正常运行通过；旧 setup 不会覆盖正式机甲场景。最终活动场景为已保存的 SampleScene，处于非 Play，Console 无错误/警告。

## 7. 用户追加的 TMP 导入整理

- [x] 7.1 备份本次 TMP 导入并核对实际引用，恢复导入器对旧字体 meta 的非功能性覆盖；验证原字体 GUID 和内容保持。
- [x] 7.2 经 AssetDatabase 将所用 TMP 字体/Shader 及许可归入 ThirdParty/UI/TextMeshPro，清理无引用示例与重复配置；验证迁移 GUID、项目依赖、根目录及唯一运行 TMP Settings。
- [x] 7.3 更新 Editor 字体路径和共享资产文档，完成编译、保存引用和实际字体/UI 回归；保留用户验收事实及本次整理证据后再归档。

## Final acceptance and archive — 2026-09-19

用户明确确认“验收全部通过”，授权完成勾选、文档同步、归档、阶段提交与推送；并追加按资产规则整理 TMP 导入。原 26 项加资产收口 3 项共 29/29 完成。TMP 15 份原资源内容一致、迁移 GUID 保持、唯一项目运行配置，以及迁移后 64 项保存审计、66 项实际运行与 180 帧干净启停复验通过。新增资源已保留许可，未升级包。

三份 delta 已完整同步主规格：新增 enemy-presentation（5 项要求）、enemy-status-ui（4 项要求），enemy-damage 增加 1 项生命周期要求；原有伤害/死亡要求和场景保持。通过主规格/本 change 严格校验后归档；缓存、录像与备份保持忽略，不修改旧归档的验收历史。当前人物小阶段完成，下一步计划留待后续讨论。
